// user status
export interface userLoginStatus {
  isLogged: boolean;
  token: string;
}

// user profile
export interface userProfileInformation {
  email: string;
  firstName: string;
  lastName: string;
  createdAt: string;
  updatedAt: string;
  id: string;
}

